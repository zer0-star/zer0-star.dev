{
  "name": "zer0-star.dev",
  "version": "0.1.0",
  "description": "awoo",
  "main": "index.js",
  "scripts": {
    "format": "npx purs-tidy format-in-place \"**/**/*.purs\"",
    "watch:spago": "spago build",
    "watch:esbuild": "node ./scripts/watch.mjs",
    "watch": "run-p watch:*",
    "build:spago": "spago build",
    "build:esbuild": "node ./scripts/build.mjs",
    "build": "run-s build:*"
  },
  "author": "zer0-star",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.15.5",
    "npm-run-all": "^4.1.5",
    "purs-tidy": "^0.9.2"
  }
}
